{
  "name": "inventory-root",
  "private": true,
  "scripts": {
    "start": "pnpm --prefix ui start",
    "build": "pnpm --prefix ui build",
    "watch": "pnpm --prefix ui watch",
    "test": "pnpm --prefix ui test",
    "prepare": "husky",
    "lint:frontend": "pnpm --prefix ui lint:frontend",
    "lint:backend": "uv run ruff check . --fix && uv run ruff format .",
    "lint": "pnpm run lint:backend && pnpm run lint:frontend"
  }
}
